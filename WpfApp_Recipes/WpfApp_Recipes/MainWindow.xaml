<Window x:Class="WpfApp_Recipes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_Recipes"
        mc:Ignorable="d"
        Title="Мои рецепты" Height="500" Width="1000" MinHeight="300" MinWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Text="{Binding ElementName=MainFrame, Path=Content.Title}" FontSize="24" Margin="10,0" VerticalAlignment="Center"></TextBlock>

        <StackPanel Grid.RowSpan="2" Background="LightGreen" Orientation="Vertical">
            <Button Name="BtnDishes" Click="BtnDishes_Click">Блюда</Button>
            <Button Name="BtnIngredients" Click="BtnIngredients_Click">Ингредиенты</Button>
        </StackPanel>

        <Frame Grid.Row="1" Grid.Column="1" Name="MainFrame" Background="LightYellow" NavigationUIVisibility="Hidden"></Frame>
    </Grid>
</Window>
